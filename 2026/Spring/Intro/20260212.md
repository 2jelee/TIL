# TIL — {{2026-02-12}}

## 📌 Today Summary
- 오늘 배운 것 한 줄 요약 : 헷갈리는 부분 재정리
- 핵심 키워드:
  - 
    - Gradle
    - HTTP
    - 테스트 코드
    - Lombok
    - Jackson

---

## 🎯 Today’s Focus
- 오늘의 학습 주제: Spring 입문 (이론 위주)
- 왜 이걸 공부했는가: 테스트 코드 알아보기
- 기대했던 것: 테스트 코드를 작성하여 단위테스트를 수행하기

---

## 📘 What I Learned
### 핵심 개념
- Gradle : 빌드 자동화 시스템 (Java 코드 -> 실행가능한 jar 파일로)
- build.gradle : Gradle 기반의 빌드 스크립트
- application.properties : 서버 포트, DB 연결 등 스프링 설정값 관리
- IP & Port : 서버 위치 주소(IP)와 특정 프로세스를 구분하는 통로(Port)
- HTTP: 데이터를 주고받는 통신 규약 
  - Method: GET(리소스 조회), POST(데이터 생성/게시) 등 
  - 구성: Header(메타 데이터), Body(실제 데이터)
- HTTP 상태 코드 (Status Code)
  - 1xx (Informational) : 요청 수신됨. 처리가 계속되고 있음
  - 2xx (Successful) : 클라이언트의 요청이 성공적으로 처리됨 (요청한 데이터가 서버에서 제공됨)
  - 3xx (Redirection) : 클라이언트가 추가적인 조치를 취해야함 (페이지 이동, 리다이렉션 등 사용)
  - 4xx (Client Error) : 클라이언트의 잘못된 요청, 인증 오류 등 (주로 404 => 클라이언트가 요청한 페이지, 리소스 서버에서 찾을 수 없음)
  - 5xx (Server Error) : 서버에 오류가 발생 (주로 서버 오류, 서버 과부화 등 | 500 => 서버 내부 오류 발생)
- Header : 처리 방법 설명, 인증, 데이터 형식, 캐시 정책
- Body : 실제 데이터, JSON / 파일 / 폼
- GET 방식 : 조회, Query 파라미터, Body 거의 없음, 캐싱 가능 
- POST 방식 : 생성/전송, Body 사용, 캐싱 없음
- Jackson : JSON 데이터 구조를 처리하는 라이브러리 (Object 타입 -> JSON 타입의 String 변환 O / JSON 타입의 String -> Object로 변환 O)
- Spring MVC 패턴
  - Model: 데이터 및 비즈니스 로직 담당. DB 연동 
  - View: 사용자 인터페이스(UI) 담당
  - Controller: 모델과 뷰 사이의 상호작용 제어. 사용자 요청 처리
  - DispatcherServlet: 모든 요청을 분석하여 적절한 컨트롤러에 전달하는 Front Controller 역할 
- Controller 활용
  - @Controller: 해당 클래스가 컨트롤러임을 명시 
  - @RequestMapping: 중복되는 URL 경로를 클래스 단위로 공통 관리 
  - @GetMapping, @PostMapping: HTTP Method별 API 경로 매핑 
  - @ResponseBody: 메서드 반환 값을 뷰 탐색 없이 응답 본문에 직접 전달


### 중요한 포인트
- HTTP에 대한 지식 (상태 코드, Header, Body)
- 직접 JSON 데이터 처리해야할 경우 Jackson 라이브러리의 "ObjectMapper" 사용 (writeValueAsString 메서드)

### 인상 깊은 문장 / 내용
>

---

## 🧠 Insight
- 새롭게 이해한 점 : Jackson 라이브러리
- 관점이 바뀐 부분 : HTTP
- 이전에 알던 것과의 차이 : 헷갈리는 부분 재정리

---

## 🛠 How I Can Use This
- 실생활 / 업무 / 공부에 어떻게 적용할 수 있는가 : 개발 시 문제 발생했을 경우 빠른 확인과 대처
- 바로 써먹을 수 있는 방법 : 개발자 도구 활용법