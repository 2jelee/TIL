package ex07.randomClass;

import java.util.Random;
import java.util.Scanner;

/*
[문제1] up/down 게임 프로그램 작성 (난수 이용)
1~100 숫자 중에서 임의의 난수 추출해놓고 사용자는 5번의 기회를 주어 입력받는다.
그 입력받은 숫자와 추출한 난수가 같은지 판정하는 프로그램 작성
못맞추면 답도 알려줘라.

문제1] 업/다운 게임 프로그램 작성 (난수 이용)
1~100 숫자 중에서 임의의 난수 추출해 놓고, 사용자는 5번의 기회를줘서 입력 받는다.  
그 입력 받은 숫자와 추출한 난수가 같은지를 판정하는 프로그램 작성
*/

public class Quiz_upDown {
	/*
	public static void main(String[] args) {
		int temp=0;	//난수를 저장할 변수
		Random rand = new Random(); 
		temp = rand.nextInt(100)+1; //1~100
		
		System.out.print("값을 입력하세요(1) : ");
		Scanner sc = new Scanner(System.in);
		int user=0; //user가 입력한 값을 저장할 변수
		user = sc.nextInt();
//		for(int i=1; i<6; i++) {	//user 입력 5번의 기회
//		}
		if(temp==user) {
			System.out.println("성공입니다.");
		}
		if(temp!=user) { 
			System.out.print("재도전하세요(2) : "); 
			user = sc.nextInt();		 
		} 
		if(temp!=user) { 
			System.out.print("재도전하세요(3) : "); 
			user = sc.nextInt();							
		} 
		if(temp!=user) { 
			System.out.print("재도전하세요(4) : "); 
			user = sc.nextInt();				
		} 
		if(temp!=user) { 
			System.out.print("재도전하세요(5) : "); 
			user = sc.nextInt();				
		} 
		System.out.println("난수는 "+ temp + "였습니다."); 
	}
	*/
	
	/*******************************************************/
	public static void main(String[] args) {
		
	}
}
